# 大帅网关 — 发给别人怎么用

对方收到 `DashuaiGateway.exe` 后，**不用改代码**，按下面做即可在 WorkBuddy 里用。

## 前置：账号与卡密

1. 打开软件后先阅读并同意 **免责声明**
2. **注册 / 登录** 普通用户账号
3. 在软件内 **购买套餐**（天卡 ¥3 / 月卡 ¥20 / 季卡 ¥50 / 年卡 ¥120，或 Token 包），微信扫码支付；或 **输入已有卡密** 激活  
   - 一卡密只能绑定一个账号  
   - 支付成功一般会自动激活；失败时把邮件/查单里的卡密粘贴到「购买 / 卡密」页
4. 右上角可查看 **剩余时间** 或 **剩余 Token**

> 开发自测：`data/config.json` 里可设 `"require_license": false` 关闭门禁；正式分发请配置 `license_api_base` 并设 `"require_license": true`。

## 三步上手（激活之后）

1. **保持网关窗口开着**（不要关）
2. 打开 **「上游渠道」**，粘贴自己的 **API Key**（国内建议：魔搭 ModelScope / 硅基 SiliconFlow / 智谱 Zhipu）  
   - 粘贴后会**自动勾选启用**
3. 点 **「保存并同步 WorkBuddy」** → **完全退出并重启 WorkBuddy** → 模型列表选  
   `日常 / 快速 / 复杂 / 小说 / 代码 / 识图 / 翻译 / 总结 / 推理 / 长文 / Agent · 大帅网关`（共 **11 类用途**）

> **说明**：上游渠道可以配 10+ 个（魔搭、豆包、NVIDIA…），WorkBuddy 里只显示 **11 个用途名**，不是每个渠道一行。网关会在背后自动从所有渠道里选模型。

> **同步后看不到？** 必须 **任务栏右键 WorkBuddy → 退出**（不是只关窗口），再重新打开。可在 `%USERPROFILE%\.workbuddy\models.json` 确认是否已有 11 条「大帅网关」。

侧栏 **「新手指南」** 有傻瓜式步骤与「如何注册上游 API Key」说明。

## 注意

- 本地 API Key 在面板「本地 Key」框里显示；**点「同步 WorkBuddy」会把当前 Key 一并写入** `%USERPROFILE%\.workbuddy\models.json`，无需单独先点保存
- **消耗统计**里「上游调用」含网关重试次数；「客户端请求」按 request_id 去重。NVIDIA 等流式若不返回 usage，会按输出字数 **≈估算** Token
- 网关必须一直开着（WorkBuddy 连的是本机 `http://127.0.0.1:8010/v1`）
- 未登录或未激活时，直接调用 `/v1/chat/completions` 会被拒绝
- 国外渠道（NVIDIA / Groq / Gemini 等）通常需要 VPN
- 硅基等平台需要账户里**有余额**，否则会报额度不足
- 首次运行会在 EXE 同目录生成 `data/`（配置与 Key 只存在对方电脑，不会打进你的密钥）
- 顶部可看远程公告；「检查更新」对比远端版本并打开下载链接（需服务端配置）

## 可选：豆包 / 混元（元宝）写小说

这两个都是 **国内直连**，适合写长文，比魔搭 397B 少限流：

### 豆包（火山方舟）
1. 打开 [火山方舟控制台](https://console.volcengine.com/ark) → 创建 **推理接入点**（选 Doubao Seed 1.6 / Lite 等）
2. 网关 **「一键配置」** 勾选 **豆包(火山方舟)** → 部署
3. **「上游渠道」** 粘贴 API Key，**模型列表**里把示例 ID 换成你的 **接入点 ID**（`ep-xxxx`，在控制台接入点详情里复制）
4. **保存并同步 WorkBuddy** → 重启 WorkBuddy → 选 **小说 · 大帅网关**

### 混元（元宝同底层 API）
1. 打开 [混元 API Key 管理](https://console.cloud.tencent.com/hunyuan/api-key) → 创建 Key（`sk-` 开头）
2. **一键配置** 勾选 **混元(元宝API)** → 部署 → 粘贴 Key
3. 新用户常有免费 Token；写小说可用 `hunyuan-turbos-latest` 或免费档 `hunyuan-lite`
4. 保存并同步 → 重启 WorkBuddy

> 网页版「元宝」「豆包」App 本身不能直接把 Cookie 接到网关；必须走上面这种 **官方 API Key** 方式。

## 可选：刷新渠道模型列表

若模型过期，可在「一键配置」里勾选渠道 →「部署勾选的预设」（**不会覆盖已有 Key**），再保存并同步一次。
