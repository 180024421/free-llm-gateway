# 大帅网关 — 发给别人怎么用

对方收到安装包后，**不用装 Python、不用改代码**，打开即用。

| 系统 | 发给对方什么 | 怎么开 |
|------|-------------|--------|
| **Windows** | `DashuaiGateway.exe` | 双击；缺 WebView2 时装 [WebView2 运行时](https://developer.microsoft.com/microsoft-edge/webview2/) |
| **Mac（Apple Silicon）** | `大帅网关-mac-arm64.zip`（**仅此一种，无 dmg/快速启动**） | 解压整个文件夹 → **右键** `启动大帅网关.command` →「打开」 |

公网下载与教程：

- Windows：http://111.229.202.251/dashuai-gateway/DashuaiGateway.exe
- Mac：http://111.229.202.251/dashuai-gateway/大帅网关-mac-arm64.zip
- 上手教程（Win/Mac 分 Tab）：http://111.229.202.251/guides/dashuai-gateway-start/

> Win10 / Win11 一般已自带 **Microsoft Edge WebView2**。Mac 包自带便携 Python，**不需要** Homebrew / 系统 Python。Mac **没有** 单独的 dmg「快速启动」包，zip 解压即用。


## 你买的是什么

正式版卖的是 **网关使用权（时长卡 / Token 包）**：

- 登录 + 激活后，才能调用本机 `/v1/chat/completions`
- **上游模型算力仍用对方自己的 API Key**（魔搭 / 硅基 / 智谱等），不是把我们云端模型额度转卖出去
- Token 包会按调用用量扣减（优先上游真实 usage；没有 usage 时按字数估算并标记）
- 正式 EXE **强制开启授权**，改本地配置关掉 `require_license` 会被写回

> 若要做「云端算力 Token」（用户不用自备 Key），需要另外部署上游池 + 服务端代理，不在本安装包默认能力内。

## 前置：账号与卡密

1. 打开软件后先阅读并同意 **免责声明**（软件免费可用，但**不开源**；禁止商用 / 反编译 / 二次分发源码）
2. **注册 / 登录** 普通用户账号
3. 在软件内 **购买套餐**（天卡 / 月卡 / 季卡 / 年卡，或 Token 包），微信扫码支付；或 **输入已有卡密** 激活  
   - 一卡密只能绑定一个账号  
   - 支付成功一般会自动激活；失败时把邮件/查单里的卡密粘贴到「购买 / 卡密」页
4. 右上角可查看 **剩余时间** 或 **剩余 Token**（余量不足会提醒）
5. 需要支持可打开 **「关于 / 赞助」**：赞助收款码、QQ 交流群（群号 914577057）与作者联系方式

> 仅开发自测可用 `start.cmd` + `"require_license": false`。**发给客户的 Windows EXE / Mac 便携包始终要求登录激活。**

## 三步上手（激活之后）

1. **保持网关窗口开着**（不要关）
2. 打开 **「上游渠道」**，粘贴自己的 **API Key**（国内建议：魔搭 ModelScope / 硅基 SiliconFlow / 智谱 Zhipu）  
   - 粘贴后会**自动勾选启用**
3. 点 **「保存并同步客户端」**（会自动写入 WorkBuddy / Cursor / VS Code / IDEA 的 Base URL 和 Key）→ 在模型列表选合适路由：

| 模型 | 适合场景 | 说明 |
|------|----------|------|
| **日常 · 大杯优先（可兜底）** | 普通聊天 | 优先大模型；大杯全挂时可用免费小杯保活 |
| **复杂 · 仅大杯** | Skill / 要稳准 | 只用大杯，不落到小杯（宁失败也不幻觉） |
| **快速** | 只要快 | 小杯/Flash；可竞速换路 |
| **自定义** | 自管名单 | 面板「路由模型」新增并填写候选；可勾选是否允许小杯兜底；保存后再「同步到本机客户端」，客户端会出现「名字 · 大帅网关」（0.4.3 起） |

> Skill / Agent 任务请选 **复杂**，不要用 **快速**。改完候选后务必点「保存路由」（会自动锁定，避免被定时优选覆盖）。

> **一般不用改客户端设置。** WorkBuddy 正在运行时列表常会自动刷新；若没看到新模型，完全退出再打开一次即可。

## 路由被覆盖？怎么锁定

面板「路由模型」：

1. 改「候选模型」或「失败后允许免费小杯兜底」
2. 点 **保存路由**（自动开启「锁定手动路由」）
3. 之后健康探测 / 保存渠道 / 同步客户端 **不会再改你的名单**
4. 若要恢复智能优选：点「按日志优选」并确认（会覆盖候选，然后重新锁定）

也可用页面上的「锁定手动路由」开关随时开关。

## 路由页进阶（0.4.4）

- **大杯 / 小杯标注**：候选旁会标「大杯 / 中杯 / 小杯」；也可点选添加，避免把小杯误塞进「复杂」
- **近 24h 命中**：每张路由卡片显示成功次数、实际多走哪家模型、兜底几次
- **导入 / 导出**：顶栏「导出」下载 JSON；「导入」可合并或整表替换（建议先导出备份）
- **每路由超时 / 重试**：可选填「超时秒 / 卡顿切换秒 / 最大重试」，覆盖该路由的默认等待策略
- **请求级禁兜底**：调用时加请求头 `X-Gateway-Fallback-Policy: none`（或 `free_pool`），临时覆盖该次请求的兜底策略

## 怎么知道这次是不是小杯兜底答的

- 工作台「最近上游」卡片：兜底时显示黄字「本次由免费小杯兜底」，并累计本次运行的兜底次数
- 路由卡片「近 24h」会累计该路由的兜底次数
- 响应头带 `X-Gateway-Fallback: free-pool`（脚本 / 客户端可据此判断）
- 兜底频繁 = 大杯 Key 额度或可用性有问题，先去「上游渠道」查

在线教程（含选模型 / 防幻觉一节）：http://111.229.202.251/guides/dashuai-gateway-start/#routes

## 安装包使用提示

### Windows

1. 安装/首次启动会生成 `%LocalAppData%\大帅网关\data\`（或安装目录下 `data\`）
2. 登录激活 → 配上游 Key → 同步到本机客户端
3. 更新：面板点「检查更新」，或覆盖安装新版 EXE（一般保留 data）
4. 卸载：开始菜单「卸载 大帅网关」；彻底清除可再删 `data\`

### Mac

1. 解压后保持文件夹完整；数据在解压目录 `data/`（日志：`data/desktop.log`）
2. 首次启动会自动准备环境（通常只需一次；离线 wheels 不全时会联网补）
3. 登录激活 → 配上游 Key → 同步到本机客户端（WorkBuddy / Cursor 须装在同一台 Mac）
4. 更新：下载新 zip，解压到新目录；需要旧配置时把旧 `data/` 拷过去
5. 未 Apple 签名时，第一次务必「右键 → 打开」

## 注意

- 本地 API Key 会随「同步到本机客户端」写入 WorkBuddy / Cursor 等；换 Key 后请再点一次同步
- 占位 Key（如 `sk-local-change-me`）**不会**自动覆盖客户端；正式包首次会自动生成本地 Key
- 未登录 / 未激活 / Token 用尽时，直接调用 chat 会被拒绝
- 离线宽限较短（正式版约 2 小时）；长期离线需重新联网校验
- 国外渠道通常需要 VPN；硅基等平台账户需有余额
- **切勿**把本机真实 `data/config.json` / `providers.json` 打进发给别人的安装包
