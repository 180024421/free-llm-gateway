# AGENTS.md — dashuai-gateway

> **多仓工作区说明**：Cursor 会把各仓库根目录 `AGENTS.md` **全局注入**所有对话。长规范已迁到按需规则，避免串仓/拖慢。

**本仓库 Agent 契约（按需加载）**：[`.cursor/rules/00-agents.mdc`](./.cursor/rules/00-agents.mdc)

打开/编辑本仓库文件，或明确提到本项目时，Agent 会加载该规则。详细规范仍在仓库 `docs/` 与其余 `.cursor/rules/`。
